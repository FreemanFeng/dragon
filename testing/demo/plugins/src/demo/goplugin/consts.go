package goplugin

// Ops
const (
	DOT   = "."
	COLON = ":"
	SLASH = "/"
)

// Paths
const (
	PathRoot       = "/"
	PathInit       = "/init"
	PathRunService = "/run/:service"
)

// Services
const (
	INIT = "init"
	RUN  = "run"
)

const (
	ParamService = "service"
)

const (
	OK     = "ok"
	EMPTY  = ""
	CTJSON = "application/json;chartset=uft-8"
)
